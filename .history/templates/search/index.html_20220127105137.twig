{% extends "base.html.twig" %}
{% block body %}
<h1>Rechercher </h1>
    {% for search in searchs %}
        <div>
            
            <p>Catégories : {{ advert.category }}
            <p>Produits : {{ advert.product }}</p>
             
                  <div class="col-12">
                 <img class="img-fluid rounded" src="{{ advert.owner.photo.url }}">
             </div>
            {# Lien vers la fiche de l'annonce : #}
            <a href="{{ path("product_show", {id: pr.id}) }}">Voir la fiche</a>
        </div>
    {% else %}
        Aucun article correspond à votre recherche.
    {% endfor %}